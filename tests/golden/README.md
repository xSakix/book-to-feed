# Golden files

Expected segmentation output per fixture (#24). These are checked in on purpose:
a diff here is the whole point — it shows exactly which posts moved when a
scoring weight in the segmenter changes.

Regenerate with `npm run test -- -u`, and read the diff before accepting it.
